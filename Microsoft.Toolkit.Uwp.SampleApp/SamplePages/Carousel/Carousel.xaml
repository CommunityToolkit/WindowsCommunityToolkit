<Page x:Class="Microsoft.Toolkit.Uwp.SampleApp.SamplePages.Carousel"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
      xmlns:local="using:Microsoft.Toolkit.Uwp.SampleApp.SamplePages"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d">


 

    <Grid Background="{StaticResource Brush-Grey-05}">

        <Border Margin="0"  >

            <controls:Carousel 
                        SelectedIndex="{Binding SelectedIndex.Value, Mode=OneWay}"
                        TransitionDuration="{Binding TransitionDuration.Value, Mode=OneWay}" 
                        Depth="{Binding Depth.Value, Mode=OneWay}" 
                        MaxVisibleItems="{Binding MaxVisibleItems.Value, Mode=OneWay}"
                        x:Name="carouselControl"
                        Rotation="{Binding Rotation.Value, Mode=OneWay}" 
                        TranslateY="{Binding TranslateY.Value, Mode=OneWay}"
                        TranslateX ="{Binding TranslateX.Value, Mode=OneWay}">
                <controls:Carousel.EasingFunction>
                    <CubicEase EasingMode="EaseOut" />
                </controls:Carousel.EasingFunction>
                <controls:Carousel.ItemTemplate>
                    <DataTemplate>
                        <Grid >
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Image Source="{Binding BitmapImage}" Width="400" Height="300" VerticalAlignment="Bottom" 
                               Stretch="Uniform"></Image>
                        </Grid>
                    </DataTemplate>
                </controls:Carousel.ItemTemplate>

            </controls:Carousel>
        </Border>

        

    </Grid>
</Page>
