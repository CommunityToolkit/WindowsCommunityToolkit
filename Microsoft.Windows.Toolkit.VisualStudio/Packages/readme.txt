*** TODO ***

windowsappstudio.dataproviders.1.3.0.nupkg should be replaced by Microsoft.Windows.Toolkit.Services nupkg when it has been built/published.  If we want to interim, we can point to the DLL instead, it just won't auto-update in the target solution.
