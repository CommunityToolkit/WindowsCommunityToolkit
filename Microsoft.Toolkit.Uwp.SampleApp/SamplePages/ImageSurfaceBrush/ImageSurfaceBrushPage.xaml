<Page x:Class="Microsoft.Toolkit.Uwp.SampleApp.SamplePages.ImageSurfaceBrushPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:media="using:Microsoft.Toolkit.Uwp.UI.Media"
      mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="46" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="200" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="1"
                       Margin="4,0"
                       VerticalAlignment="Center"
                       Text="Stretch: " />
            <ComboBox x:Name="StretchOptions"
                      Grid.Column="2"
                      Width="100"
                      Margin="0,4" />
            <TextBlock Grid.Column="3"
                       Margin="8,0,4,0"
                       VerticalAlignment="Center"
                       Text="Horizontal Alignment: " />
            <ComboBox x:Name="HAOptions"
                      Grid.Column="4"
                      Width="100"
                      Margin="0,4" />
            <TextBlock Grid.Column="5"
                       Margin="8,0,4,0"
                       VerticalAlignment="Center"
                       Text="Vertical Alignment: " />
            <ComboBox x:Name="VAOptions"
                      Grid.Column="6"
                      Width="100"
                      Margin="0"
                      VerticalAlignment="Center" />
        </Grid>
        <Grid x:Name="XamlRoot"
              Grid.Row="1"
              Visibility="Collapsed" />
        <Grid Grid.Row="1">
            <Grid.Background>
                <media:ImageSurfaceBrush Source="ms-appx:///Assets/Photos/SpeedTripleAtristsPoint.jpg"
                                         SurfaceHeight="500"
                                         SurfaceWidth="900">
                    <media:ImageSurfaceBrush.ImageOptions>
                        <media:ImageSurfaceOptions x:Name="ImageOptions"
                                                   HorizontalAlignment="Center"
                                                   VerticalAlignment="Center"
                                                   Stretch="None" />
                    </media:ImageSurfaceBrush.ImageOptions>
                </media:ImageSurfaceBrush>
            </Grid.Background>
        </Grid>
    </Grid>
</Page>
