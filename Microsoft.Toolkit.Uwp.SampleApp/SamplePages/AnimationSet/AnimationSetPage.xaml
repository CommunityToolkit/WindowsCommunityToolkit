<Page
    x:Class="Microsoft.Toolkit.Uwp.SampleApp.SamplePages.AnimationSetPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Microsoft.Toolkit.Uwp.SampleApp.SamplePages.AnimationSet"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="60"></ColumnDefinition>
                <ColumnDefinition Width="200"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="40"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>

            <StackPanel Grid.ColumnSpan="3" Orientation="Horizontal">
                <Button Click="StartClicked">
                    <SymbolIcon Symbol="Play"></SymbolIcon>
                </Button>
                <Button Click="StopClicked">
                    <SymbolIcon Symbol="Stop"></SymbolIcon>
                </Button>
                <Button Click="ResetClicked">
                    <SymbolIcon Symbol="Rotate"></SymbolIcon>
                </Button>
            </StackPanel>
            
            <ListView x:Name="AnimationFunctionList" Grid.Row="1" IsItemClickEnabled="True" SelectionMode="None">
            </ListView>

            <ListView Grid.Column="1" Grid.Row="1">
                
            </ListView>

            <Rectangle x:Name="Element" Height="100" Width="100" Grid.Row="1" Grid.Column="2" Fill="Red"></Rectangle>
        </Grid>
    </Grid>
</Page>
