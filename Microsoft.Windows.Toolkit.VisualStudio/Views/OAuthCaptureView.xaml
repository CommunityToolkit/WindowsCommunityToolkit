<UserControl x:Class="Microsoft.Windows.Toolkit.VisualStudio.Views.OAuthCaptureView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Microsoft.Windows.Toolkit.VisualStudio.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

    <StackPanel>
            <TextBlock>
                <Hyperlink NavigateUri="{Binding ConnectedServiceInstance.DataProviderModel.ServiceDeveloperInformationUrl, Mode=OneWay}" RequestNavigate="Hyperlink_RequestNavigate">
                    <Hyperlink.Inlines>
                        <Run Text="Service Developer Portal" />
                    </Hyperlink.Inlines>
                </Hyperlink>
            </TextBlock>
        
        <Grid x:Name="DynamicGrid">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
        </Grid>
        
        <StackPanel Orientation="Horizontal">
            <Button Content="Ok" Command="{Binding OkCommand}" />
            <Button Content="Cancel" Command="{Binding CancelCommand}" />
        </StackPanel>
        
    </StackPanel>
    
</UserControl>
