<Page x:Class="Microsoft.Toolkit.Uwp.SampleApp.SamplePages.AdaptiveGridViewPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
      xmlns:converters="using:Microsoft.Toolkit.Uwp.UI.Converters"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d">

    <Page.Resources>
        <DataTemplate x:Key="PhotosTemplate">
            <Grid Background="White"
                  BorderBrush="Black"
                  BorderThickness="1">
                <Image HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       Source="{Binding Thumbnail}"
                       Stretch="UniformToFill" />
            </Grid>
        </DataTemplate>

        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock x:Name="SelectedItemCountTextBlock"
                   Margin="0,12"
                   Grid.ColumnSpan="2"
                   HorizontalAlignment="Center"
                   FontFamily="Segoe UI Light"
                   FontSize="21"
                   Text="You haven't selected any items"
                   Visibility="{Binding IsItemClickEnabled.Value, Converter={StaticResource BoolToVisibilityConverter}}" />

        <Grid x:Name="XamlRoot" Grid.ColumnSpan="2" Grid.Row="1" />

        <TextBlock FontFamily="Segoe UI Light"
                   FontSize="21" 
                   Text="Number of Photos" 
                   Grid.Row="2" 
                   Margin="5" />
        
        <Slider x:Name="NumberSlider" Grid.Row="2" Grid.Column="1" Margin="30,5" StepFrequency="1"   />
    </Grid>
</Page>