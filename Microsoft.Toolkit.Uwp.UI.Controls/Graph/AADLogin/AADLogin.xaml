<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls.Graph">

    <Style TargetType="controls:AadLogin">
        <Setter Property="BorderBrush" Value="{ThemeResource SystemControlForegroundTransparentBrush}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="controls:AadLogin">
                    <StackPanel>
                        <Button x:Name="btnMain" Background="Transparent">
                            <Button.Content>
                                <controls:ProfileCard 
                                    DisplayMode="{TemplateBinding View}" 
                                    DefaultImage="{TemplateBinding DefaultImage}" 
                                    UserId="{TemplateBinding CurrentUserID}"  
                                    GraphAccessToken="{TemplateBinding GraphAccessToken}"
                                    DefaultTitleText="{TemplateBinding SignInDefaultText}" 
                                    DefaultMailText="{TemplateBinding SignInDefaultText}" />
                            </Button.Content>
                        </Button>
                    </StackPanel>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
