<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Microsoft.Toolkit.Uwp.UI.Controls">
    <Style x:Name="MessageStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="Foreground" Value="Red"/>
    </Style>
    <Style TargetType="local:CameraPreview" >
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:CameraPreview">
                    <Grid Background="{TemplateBinding Background}">
                        <MediaPlayerElement x:Name="MediaPlayerElementControl" MinWidth="300" Width="400" HorizontalAlignment="Left">
                        </MediaPlayerElement>
                        <Button x:Name="FrameSourceGroupButton" Background="Transparent" 
                                VerticalAlignment="Top" HorizontalAlignment="Left" Margin="10">
                            <Image Source="{TemplateBinding FrameSourceGroupButtonIcon}" Width="20"></Image>
                        </Button>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>

