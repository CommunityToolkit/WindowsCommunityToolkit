<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls">

    <Style TargetType="controls:SlidableListItem">
        <Setter Property="LeftForeground" Value="White" />
        <Setter Property="RightForeground" Value="White" />
        <Setter Property="VerticalAlignment" Value="Stretch" />
        <Setter Property="Background" Value="White" />
        <Setter Property="ActivationWidth" Value="80" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="controls:SlidableListItem">
                    <Grid>
                        <Grid Margin="1"
                              x:DeferLoadStrategy="Lazy"
                              x:Name="CommandContainer">
                            <StackPanel HorizontalAlignment="Left"
                                        VerticalAlignment="Center"
                                        x:Name="LeftCommandPanel"
                                        Orientation="Horizontal">
                                <SymbolIcon Foreground="{TemplateBinding LeftForeground}" Symbol="{TemplateBinding LeftIcon}" />
                                <TextBlock Margin="10,0,0,0"
                                           FontSize="15"
                                           Foreground="{TemplateBinding LeftForeground}"
                                           Text="{TemplateBinding LeftLabel}" />
                                <StackPanel.RenderTransform>
                                    <CompositeTransform />
                                </StackPanel.RenderTransform>
                            </StackPanel>

                            <StackPanel HorizontalAlignment="Right"
                                        VerticalAlignment="Center"
                                        x:Name="RightCommandPanel"
                                        Orientation="Horizontal">
                                <TextBlock Margin="0,0,10,0"
                                           FontSize="15"
                                           Foreground="{TemplateBinding RightForeground}"
                                           Text="{TemplateBinding RightLabel}" />
                                <SymbolIcon Foreground="{TemplateBinding RightForeground}" Symbol="{TemplateBinding RightIcon}" />
                                <StackPanel.RenderTransform>
                                    <CompositeTransform />
                                </StackPanel.RenderTransform>
                            </StackPanel>
                        </Grid>
                        <Grid x:Name="ContentGrid"
                              Background="{TemplateBinding Background}"
                              ManipulationMode="TranslateX,System">
                            <Grid.RenderTransform>
                                <CompositeTransform x:Name="ContentTransform" />
                            </Grid.RenderTransform>
                            <ContentPresenter />
                        </Grid>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
