<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Microsoft.Toolkit.Uwp.UI.Controls">
    <Style TargetType="local:HamburgerButton" >
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:HamburgerButton" >
                    <Border 
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        Width="{TemplateBinding Width}"
                        Height="{TemplateBinding Height}"
                        >
                        <Canvas x:Name="ContainerCanvas">

                            <Rectangle x:Name="Stripe1"  Canvas.Left="0" Canvas.Top="0" Fill="{TemplateBinding Foreground}"/>
                            <Rectangle x:Name="Stripe2"  Canvas.Left="0" Canvas.Top="0" Fill="{TemplateBinding Foreground}"/>
                            <Rectangle x:Name="Stripe3"  Canvas.Left="0" Canvas.Top="0"  Fill="{TemplateBinding Foreground}"/>
                        </Canvas>
                    </Border>

                </ControlTemplate>
            </Setter.Value>

        </Setter>
    </Style>
</ResourceDictionary>
