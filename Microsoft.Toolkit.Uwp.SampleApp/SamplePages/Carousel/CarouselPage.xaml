<Page
    x:Class="Microsoft.Toolkit.Uwp.SampleApp.SamplePages.CarouselPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Microsoft.Toolkit.Uwp.SampleApp.SamplePages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    xmlns:data="using:Microsoft.Toolkit.Uwp.SampleApp.Data"
    mc:Ignorable="d">

    <Grid Name="Root" Background="White">
        <controls:Carousel x:Name="CarouselControl" 
                           ItemsSource="{x:Bind items, Mode=OneWay}" 
                           Orientation="{Binding DataContext.Orientation.Value, ElementName=Root, Mode=OneWay}">
            
            <controls:Carousel.ItemTemplate>
                
                <DataTemplate x:DataType="data:PhotoDataItem">
                    <!-- CarouselItem is automaticaly generated if not specified (not required if properties or events are not needed -->
                    <controls:CarouselItem CarouselItemLocationChanged="CarouselItem_CarouselItemLocationChanged"
                                           DefaultAnimationEnabled="{Binding DataContext.DefaultAnimationEnabled.Value, ElementName=Root, Mode=TwoWay}">
                        <StackPanel Width="200">
                            <Image Source="{x:Bind Thumbnail}" Width="200" Height="180" Stretch="UniformToFill"></Image>
                            <controls:DropShadowPanel ShadowOpacity="0.4" Color="Black" BlurRadius="10" Margin="5,0">
                                <Border Background="LightGray">
                                    <TextBlock x:Name="Text" Text="{x:Bind Title}" VerticalAlignment="Center" TextAlignment="Center" HorizontalAlignment="Center" Foreground="DarkGray" FontSize="18" FontFamily="Segoe UI Light" TextTrimming="CharacterEllipsis"></TextBlock>
                                </Border>
                            </controls:DropShadowPanel>
                        </StackPanel>
                    </controls:CarouselItem>
                </DataTemplate>
                
            </controls:Carousel.ItemTemplate>
            
        </controls:Carousel>
    </Grid>
</Page>
