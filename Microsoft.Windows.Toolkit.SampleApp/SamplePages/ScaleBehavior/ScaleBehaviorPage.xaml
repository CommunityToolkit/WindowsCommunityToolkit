<Page
    x:Class="Microsoft.Toolkit.Uwp.SampleApp.SamplePages.ScaleBehaviorPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:behaviors="using:Microsoft.Toolkit.Uwp.UI.Animations.Behaviors"
    xmlns:core="using:Microsoft.Xaml.Interactions.Core"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Rectangle x:Name="MyRectangle" Fill="red" Height="100" Width="100" >
            <interactivity:Interaction.Behaviors>
                <behaviors:Scale x:Name="Scale" 
                                 ScaleX="{Binding ScaleX.Value, Mode=TwoWay}" 
                                 ScaleY="{Binding ScaleY.Value, Mode=TwoWay}" 
                                 ScaleZ="{Binding ScaleZ.Value, Mode=TwoWay}" 
                                 CenterX="{Binding CenterX.Value, Mode=TwoWay}" 
                                 CenterY="{Binding CenterY.Value, Mode=TwoWay}" 
                                 CenterZ="{Binding CenterZ.Value, Mode=TwoWay}" 
                                 Duration="{Binding Duration.Value, Mode=TwoWay}" 
                                 Delay="{Binding Delay.Value, Mode=TwoWay}" 
                                 AutomaticallyStart="{Binding AutomaticallyStart.Value, Mode=TwoWay}"/>
            </interactivity:Interaction.Behaviors>
        </Rectangle>
        <StackPanel HorizontalAlignment="Right" VerticalAlignment="Bottom">
            <Button Content="Apply" Margin="10">
                <interactivity:Interaction.Behaviors>
                    <core:EventTriggerBehavior EventName="Click">
                        <core:CallMethodAction 
                            TargetObject="{Binding ElementName=Scale}" 
                            MethodName="StartAnimation"/>
                    </core:EventTriggerBehavior>
                </interactivity:Interaction.Behaviors>
            </Button>
        </StackPanel>
    </Grid>
</Page>
