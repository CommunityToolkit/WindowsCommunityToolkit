<Page
    x:Class="Microsoft.Windows.Toolkit.SampleApp.SamplePages.FlipTilePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.Windows.Toolkit.UI.Controls"
    mc:Ignorable="d">

    <Page.Resources>
        <DataTemplate x:Key="PhotoTemplate">
            <Grid>
                <Image Source="{Binding Thumbnail}" Stretch="UniformToFill"/>
                <Border VerticalAlignment="Bottom">
                    <Border.Background>
                        <LinearGradientBrush EndPoint="0,1">
                            <GradientStop Offset="0" Color="Transparent" />
                            <GradientStop Offset="1" Color="#33000000" />
                        </LinearGradientBrush>
                    </Border.Background>
                    <TextBlock Text="{Binding Category}" VerticalAlignment="Bottom" Margin="5,20,5,5" />
                </Border>
            </Grid>
        </DataTemplate>
    </Page.Resources>
    <Grid>
        <StackPanel>
        <StackPanel Orientation="Horizontal">

                <controls:LiveTile Background="LightGray" x:Name="Tile1" 
                               ItemsSource="{x:Bind Photos}" Direction="Up" 
                               Width="400" Height="200" Margin="20"
                               ItemTemplate="{StaticResource PhotoTemplate}" />

                <controls:LiveTile Background="LightGray" x:Name="Tile2"
                               ItemsSource="{x:Bind Photos}" Direction="Left"
                               Width="200" Height="200" Margin="20"
                               ItemTemplate="{StaticResource PhotoTemplate}"/>

            </StackPanel>

            <StackPanel Orientation="Horizontal">
                <Border Background="White" Padding="5" Width="400" Height="50" Margin="20,0" >
                    <TextBlock Text="{Binding ElementName=Tile1, Path=SelectedItem.Title}" TextWrapping="Wrap" VerticalAlignment="Top" />
                </Border>
                <Border Background="White" Padding="5" Width="200" Height="50" Margin="20,0">
                        <TextBlock Text="{Binding ElementName=Tile2, Path=SelectedItem.Title}" TextWrapping="Wrap" VerticalAlignment="Top" />
                </Border>
            </StackPanel>
            
        </StackPanel>
    </Grid>
</Page>
